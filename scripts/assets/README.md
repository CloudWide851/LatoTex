# Release preparation assets

This directory contains the compressed, pinned Tectonic offline seed needed to
reconstruct ignored Windows runtime resources in GitHub Actions. Large
executables and the full TeX bundle are downloaded from fixed upstream HTTPS
URLs and verified before packaging.
